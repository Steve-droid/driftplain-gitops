apiVersion: v2
name: home-server-heartbeat
description: >-
  E21/HM5 — pings the external availability monitor's heartbeat URL only while the home
  Prometheus reports no firing critical alert and the public staging URLs answer through
  Cloudflare with their keywords. Suspended until the URL is sealed.
type: application
version: 0.2.0
appVersion: "1.0.0"
